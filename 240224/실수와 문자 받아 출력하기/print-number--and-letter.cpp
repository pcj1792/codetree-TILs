#include <iostream>
#include <string>
using namespace std;

int main() {
    double a,b;
    string c;
    cin >> c;
    cin >> a;
    cin >> b;
    cout.precision(2);
    cout << c << endl << fixed << a<< endl << b;// 여기에 코드를 작성해주세요.
    return 0;
}