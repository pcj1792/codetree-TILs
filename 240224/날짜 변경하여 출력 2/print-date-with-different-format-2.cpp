#include <iostream>
#include <string>
using namespace std;

int main() {
    int mm, dd, yyyy;
    cin >> mm;
    cin.get();
    cin >> dd;
    cin.get();
    cin >> yyyy;
    cout << yyyy <<"."<<mm<< "."<< dd;
    // 여기에 코드를 작성해주세요.
    return 0;
}