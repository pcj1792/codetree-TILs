#include <iostream>
using namespace std; 

int main() {
    double a, b, c;
    cin >> a;
    cin >> b;
    cin >> c;
    cout.precision(3);
    cout << fixed << a << endl << b << endl << c;// 여기에 코드를 작성해주세요.
    return 0;
}